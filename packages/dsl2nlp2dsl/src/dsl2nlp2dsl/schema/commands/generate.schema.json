{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "dsl2nlp2dsl/commands/generate",
  "title": "nlp2dsl command: GENERATE",
  "description": "Adopted from wellmanifest.dev/schemas/dsl-commands/generate",
  "x-adopts": "https://wellmanifest.dev/schemas/dsl-commands/generate",
  "type": "object",
  "required": ["verb", "text"],
  "properties": {
    "verb": { "const": "GENERATE" },
    "text": { "type": "string", "minLength": 1 },
    "out": { "type": "string" },
    "mode": { "enum": ["auto", "rules", "llm"], "default": "auto" }
  },
  "additionalProperties": false
}
