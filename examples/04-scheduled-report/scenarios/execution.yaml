format: nlp2dsl.execution.scenario.v1
example_id: 04-scheduled-report
title: Harmonogram dzienny

queries:
  - query: Codziennie o 9:00 generuj raport sprzedaży PDF i wyślij email do team@firma.pl
    mode: auto
    execute: true
    expect:
      status_in: [executed, incomplete]
  - query: Co poniedziałek raport HR w xlsx i wyślij do hr@firma.pl
    mode: auto
    execute: true
    expect:
      status_in: [executed, incomplete]
